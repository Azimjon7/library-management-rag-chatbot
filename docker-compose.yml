version: '3.8'
services:
  app:
    build: .
    ports:
      - "8000:8000"
    environment:
      - LLM_MODEL=gemma3:4b
      - EMBED_MODEL=nomic-embed-text
    volumes:
      - ./chroma_db:/app/chroma_db
      - ./data:/app/data
